# OfferPilot 开发分工与协作规范

> 本文用于明确课程设计期间的人员分工、开发边界、代码规范、测试流程和提交要求。
>
> 项目产品需求以《文档.md》为准；本文件只规定协作方式，不替代产品需求文档。

## 1. 项目概述

OfferPilot 是一个使用 AI 辅助用户进行模拟面试训练的 Android App。

核心流程：

```text
创建目标岗位 → 根据岗位生成面试题 → 用户回答 → AI 评分和反馈 → 查看本轮总结
```

### 1.1 当前技术栈

| 端 | 技术 |
|---|---|
| Android | Java、XML、Material Design 3、MVVM、LiveData |
| Android 数据与网络 | Room、Retrofit、OkHttp、ViewPager2、TabLayout、Navigation Component |
| 服务端 | Python、FastAPI、SQLAlchemy、PostgreSQL |
| AI 与通信 | 大模型 API、SSE、Pydantic |

### 1.2 V1 范围

V1 重点完成：岗位管理、完整练习流程、基础统计、历史记录、AI 出题和评分。

V1 暂不实现：用户登录、语音输入、多语言、社区、排行榜、简历解析和题目收藏。

## 2. 总体分工

| 成员 | 角色 | 负责模块 | 主要职责 |
|---|---|---|---|
| 成员 A（组长） | 组长、技术负责人、核心开发 | 练习模块、公共框架、项目联调 | 练习流程、关键架构、接口约定、代码审核、合并、最终验收 |
| 成员 B | 岗位模块负责人 | 岗位管理 | 岗位列表、新增岗位、岗位详情、岗位数据和自测 |
| 成员 C | 个人中心负责人、测试协助 | 个人中心与统计 | 统计、薄弱点、历史、设置、测试记录和文档整理 |

### 2.1 组长职责边界

成员 A 负责最终质量，但不代替其他成员完成所有自测。

- 组员：负责自己的功能开发、基本自测和问题说明。
- 组长：负责公共代码、方案统一、代码审核、模块联调和最终验收。
- 成员 C：协助执行测试用例并记录结果。
- 所有成员：发现问题及时报告，不直接覆盖他人的修改。

## 3. 成员 A：练习模块、服务端与项目统筹

### 3.1 练习模块范围

```text
练习 Tab → 选择题数 → 出题 → 答题 → 提交答案 → 反馈 → 下一题 → 本轮总结
```

### 3.2 负责页面和交付物

| 页面/类 | 主要内容 |
|---|---|
| `PracticeFragment` | 进行中练习、选择 3/5/10 题、开始或继续练习 |
| `QuestionFragment` | 题目、进度、多行答案输入、字数提示、提交 |
| `FeedbackFragment` | 总分、准确性、完整性、表达、点评、改进建议、参考答案 |
| `SummaryFragment` | 本轮平均分、维度得分、薄弱点、返回或再练一次 |
| `PracticeViewModel` | 保存当前题目、答案、进度和页面状态 |
| `PracticeRepository` | 后续统一处理本地数据和网络请求 |

对应布局：

```text
fragment_practice.xml
fragment_question.xml
fragment_feedback.xml
fragment_summary.xml
```

### 3.3 练习模块开发顺序

1. 使用固定数据完成选择题数。
2. 完成答题页面和答案输入。
3. 完成固定反馈页面。
4. 完成下一题和最后一题判断。
5. 完成本轮总结。
6. 使用 ViewModel 管理练习状态。
7. 使用 Room 保存草稿和练习记录。
8. 对接普通 JSON 接口。
9. 最后接入 AI 和 SSE。

### 3.4 第一版验收标准

- 可以选择 3、5、10 道题。
- 可以开始一轮练习并显示题目。
- 可以输入和提交答案。
- 可以显示固定反馈。
- 可以进入下一题。
- 最后一题进入总结，而不是继续增加题目。
- 当前题号、总题数和按钮文字正确。
- 空答案有提示，网络或解析失败时 App 不崩溃。

### 3.5 服务端与 AI 职责

成员 A 主导服务端核心实现，负责：

- FastAPI 项目结构和启动配置。
- 题目生成接口和反馈接口。
- Pydantic 请求、响应模型。
- 大模型 API 调用和 Prompt 对接。
- SSE 流式响应。
- AI 超时、重试、解析失败和降级处理。
- 与 Android 端确认接口字段和联调方式。

成员 B、成员 C 不直接修改服务端核心代码，主要协助：

- 使用 Swagger 或 Postman 验证接口。
- 提供岗位、题目和反馈测试数据。
- 记录接口问题和测试结果。

服务端第一版先返回固定 JSON，接口稳定后再接入真实 AI，最后实现 SSE 流式输出。

## 4. 成员 B：岗位管理模块

### 4.1 负责页面和交付物

| 页面/类 | 主要内容 |
|---|---|
| `JobListFragment` | 岗位列表、空状态、添加岗位、进入详情 |
| `AddJobFragment` | 职位、公司、JD 输入、校验、保存 |
| `JobDetailFragment` | 岗位信息、练习历史、薄弱点、开始练习 |
| `JobAdapter` | 岗位卡片列表展示 |
| `JobViewModel` | 岗位页面状态 |
| `JobRepository` | 后续统一处理岗位数据 |

对应布局：

```text
fragment_job_list.xml
fragment_add_job.xml
fragment_job_detail.xml
item_job.xml
```

### 4.2 开发顺序

1. 完成岗位空状态。
2. 点击“添加岗位”进入新增岗位页。
3. 完成职位名称、公司名称和 JD 输入。
4. 完成输入校验和保存。
5. 返回岗位列表并显示岗位卡片。
6. 点击岗位卡片进入岗位详情。
7. 点击“开始练习”时传递约定的岗位数据。
8. 后续接入 Room。

### 4.3 岗位模块验收标准

- 没有岗位时显示空状态和主按钮。
- 三项输入内容能够正确保存。
- 必填内容为空时有明确提示。
- 保存后可以在列表中看到新岗位。
- 点击岗位可以进入详情页。
- 启动练习时至少能传递 `jobId` 和 `questionCount`。

## 5. 成员 C：个人中心、统计与测试协助

### 5.1 负责页面和交付物

| 页面/类 | 主要内容 |
|---|---|
| `ProfileFragment` | 练习次数、平均分、薄弱点概览 |
| `StatisticsFragment` | 三个评分维度和综合得分 |
| `WeakPointFragment` | 薄弱点名称、平均分、出现次数、改善状态 |
| `HistoryFragment` | 按时间倒序显示练习历史 |
| `SettingsFragment` | 服务端地址的读取和保存 |

对应布局：

```text
fragment_profile.xml
fragment_statistics.xml
fragment_weak_point.xml
fragment_history.xml
fragment_settings.xml
item_history.xml
item_weak_point.xml
```

### 5.2 个人中心第一版验收标准

- “我的”页面可以打开。
- 可以显示模拟统计数据。
- 可以显示模拟薄弱点和历史记录。
- 统计页面至少显示准确性、完整性、表达和综合得分。
- 设置页面可以输入、保存并重新读取服务端地址。

### 5.3 测试协助任务

成员 C 按测试清单执行操作，并记录：测试编号、操作步骤、预期结果、实际结果、是否通过、截图和备注。

测试发现问题时，不直接修改他人核心代码，先按以下格式报告：

```text
问题页面：
复现步骤：
预期结果：
实际结果：
设备和系统版本：
截图或 Logcat：
```

## 6. 公共文件和公共职责

### 6.1 公共文件

以下文件由成员 A 统一维护，其他成员修改前必须在群里说明：

```text
MainActivity.java
MainPagerAdapter.java
activity_main.xml
AndroidManifest.xml
app/build.gradle.kts
libs.versions.toml
settings.gradle.kts
```

公共数据结构、Room 数据库、Retrofit 接口和 Navigation 配置也需要统一设计，不能由三个人各自创建不同版本。

### 6.2 公共文件修改申请

修改前说明：

```text
修改文件：
修改原因：
对其他模块的影响：
验证方式：
```

## 7. 目录和命名规范

### 7.1 推荐目录

```text
com.offerpilot.app
├── MainActivity.java
├── MainPagerAdapter.java
├── job
├── practice
├── profile
├── data
├── model
└── common
```

如果当前文件仍在 `com.offerpilot.app` 根包中，不要求立即移动，统一整理前先沟通，避免产生大量冲突。

### 7.2 命名规则

| 类型 | 规则 | 示例 |
|---|---|---|
| Fragment | 大驼峰 + `Fragment` | `QuestionFragment` |
| ViewModel | 大驼峰 + `ViewModel` | `PracticeViewModel` |
| 布局 | 小写下划线 | `fragment_question.xml` |
| 列表项 | `item_` 开头 | `item_job.xml` |
| 控件 ID | 控件类型 + 用途 | `button_submit_answer` |
| 字符串资源 | 页面或控件 + 用途 | `practice_submit` |

正式显示文字应优先放入 `res/values/strings.xml`，避免大量硬编码在布局或 Java 文件中。

## 8. 模块接口约定

### 8.1 岗位模块传给练习模块

启动练习时传递：

```text
jobId
jobTitle
companyName
jobDescription
questionCount
```

其中 `jobId`、`jobDescription` 和 `questionCount` 是第一版的关键字段。

### 8.2 练习完成后保存的数据

```text
practiceSessionId
jobId
practiceTime
averageScore
accuracyScore
completenessScore
expressionScore
weakPointTag
```

### 8.3 反馈数据格式

```json
{
  "scores": {
    "accuracy": 8,
    "completeness": 6,
    "expression": 7
  },
  "core_issue": "回答遗漏了关键知识点",
  "improvement": "建议补充具体原理和使用场景",
  "reference_answer": "这里是参考回答",
  "tag": "知识点掌握不完整"
}
```

字段确定后，Android 和服务端不得擅自改名；需要变更时先通知全组。

## 9. Git 协作规范

### 9.1 分支

```text
main
├── feature/practice
├── feature/job
└── feature/profile
```

成员只在自己的功能分支开发，`main` 只保留能够运行的版本。成员 A 负责审核后合并。

### 9.2 提交规范

提交格式：

```text
类型: 简短说明
```

允许的类型：`feat`、`fix`、`style`、`refactor`、`test`、`docs`。

示例：

```text
feat: add practice question page
fix: fix answer submit button
style: adjust job card layout
test: add practice flow test cases
```

### 9.3 合并流程

```text
组员开发 → 自测 → 提交分支 → 提交合并请求 → 组长审核 → 联调测试 → 合并 main
```

提交前必须确认：

- 项目可以编译并启动。
- 自己负责的页面可以打开。
- 没有误修改其他模块。
- 没有提交 `local.properties`、API 密钥或个人电脑路径。
- 已说明当前未完成内容和已知问题。

## 10. 测试与验收

### 10.1 测试责任

| 测试类型 | 负责人 | 内容 |
|---|---|---|
| 开发者自测 | 各模块负责人 | 确认自己的页面和功能基本可用 |
| 测试记录 | 成员 C 协助 | 按测试清单操作并记录结果 |
| 模块联调 | 成员 A | 检查岗位、练习、个人中心之间的数据传递 |
| 最终验收 | 成员 A | 按产品需求完整走通核心流程 |

### 10.2 核心测试清单

| 编号 | 测试内容 | 预期结果 |
|---|---|---|
| T001 | 启动 App | 正常进入岗位 Tab |
| T002 | 切换三个 Tab | 三个页面均能正常显示 |
| T003 | 新增岗位 | 列表显示新岗位 |
| T004 | 查看岗位详情 | 信息显示正确 |
| T005 | 选择 3/5/10 题 | 题目数量和进度正确 |
| T006 | 提交空答案 | 显示提示，不崩溃 |
| T007 | 提交有效答案 | 显示反馈页面 |
| T008 | 完成最后一题 | 进入本轮总结 |
| T009 | 查看统计和历史 | 数据显示正确 |
| T010 | App 重启 | 已保存的数据仍存在 |
| T011 | 断网操作 | 显示友好提示，答案不丢失 |
| T012 | AI 或 JSON 解析失败 | 降级显示，不崩溃 |

## 11. 阶段计划

### 阶段一：页面与静态流程

三人分别完成各自页面，使用模拟数据，不接 AI 和数据库。

### 阶段二：页面跳转与模块联调

完成：

```text
岗位列表 → 新增岗位 → 岗位详情 → 选择练习题数
→ 答题 → 反馈 → 下一题 → 总结 → 返回岗位详情
```

### 阶段三：本地数据

使用 Room 保存岗位、练习轮次、题目、答案和反馈，重点实现中断恢复。

### 阶段四：服务端接口

先实现普通 JSON 接口，再接入 SSE：

```text
GET    /jobs
POST   /jobs
PUT    /jobs/{id}
DELETE /jobs/{id}
POST   /practice/questions
POST   /practice/feedback
```

### 阶段五：AI、异常处理和最终验收

完成 AI 出题、AI 评分、SSE 流式显示、超时处理、解析失败降级、断网提示和最终测试。

## 12. 沟通模板

### 开始开发

```text
准备开发：
涉及文件：
预计完成时间：
是否修改公共文件：是/否
```

### 完成任务

```text
已完成：
测试结果：
修改文件：
遗留问题：
```

### 遇到问题

```text
问题页面或文件：
复现步骤：
报错内容：
已尝试方法：
截图或日志：
```

## 13. 第一阶段立即执行任务

| 成员 | 立即任务 | 完成标准 |
|---|---|---|
| 成员 A | 完成练习 Tab 的题数选择页面 | 能选择 3/5/10 题并开始模拟练习 |
| 成员 B | 完成岗位空状态和新增岗位页面 | 能打开新增页面并完成表单布局 |
| 成员 C | 完成个人中心静态页面和测试表 | 能显示模拟统计，并建立测试记录 |

第一阶段暂不要求真实 AI、SSE 或复杂数据库功能。

## 14. V1 最终验收标准

- App 可以正常启动，顶部三个 Tab 可以切换。
- 可以新增岗位并查看岗位详情。
- 可以从岗位发起练习并选择题目数量。
- 可以显示题目、输入答案、提交答案并查看反馈。
- 可以完成整轮练习并查看本轮总结。
- 可以查看基础统计、薄弱点和练习历史。
- App 重启后已保存数据不会全部丢失。
- 网络失败、AI 超时或解析失败时 App 不直接崩溃。